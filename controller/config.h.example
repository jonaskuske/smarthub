#ifndef controller_config_h
#define controller_config_h

#include "events.h"
#include "pins.h"

const char *WIFI_SSID = "My WiFi Network";
const char *WIFI_PASSWORD = "*********";

// Hostname/IP and port of the Smart Hub Server.
// The local dev server displays its IP address and port in the console.
const char *SOCKET_SERVER_ADDRESS = "smarthub.jonaskuske.de";
const int SOCKET_SERVER_PORT = 80;

#endif